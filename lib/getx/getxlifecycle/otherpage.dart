import 'package:flutter/material.dart';

class OtherPageLifecycle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("Othe Page"),
        ),
      ),
    );
  }
}
